/**
 * Service Worker แบบเรียบง่ายที่สุด
 * ------------------------------------------------------------------
 * ทำหน้าที่เพียงให้เบราว์เซอร์ถือว่าเว็บนี้ติดตั้งเป็นแอปได้
 * โดย "ไม่แคชอะไรเลย" ทุกคำขอส่งต่อไปยังเครือข่ายตามปกติ
 *
 * เหตุผลที่ไม่แคช คือเนื้อหาบทเรียนถูกเพิ่มและแก้ไขบ่อย
 * ถ้าแคชไว้ผู้เรียนจะเห็นเนื้อหาเก่าค้างหลัง deploy ใหม่
 * ซึ่งสร้างปัญหามากกว่าประโยชน์ที่ได้จากการเปิดออฟไลน์
 */

self.addEventListener('install', (e) => self.skipWaiting());

self.addEventListener('activate', (e) => e.waitUntil(self.clients.claim()));

self.addEventListener('fetch', (e) => {
  // ส่งต่อไปยังเครือข่ายตรง ๆ ไม่แตะต้องคำขอ
  return;
});
